name: basis_learning
channels:
  - pytorch
  - nvidia
  - conda-forge
dependencies:
  - python=3.12
  - pip
  - numpy
  - scipy
  - pytorch
  - psutil
  - tqdm
  - hydra-core
  - wandb
  - pip:
      - matplotlib
      - python-dotenv
      - matplotlib
      - torchvision
      - einops
      - omegaconf
      - random_word
      # formatting
      - black
      - isort
      - nflows
      # TODO: add Jupyter to the environment from the get-go
