# @package _global_
defaults:
  - /domain_samplers@domain_sampler: disk_sampler
  - /function_generator@function_generator: random_bandpass
  - /neural_isometry@neural_isometry: semilagrangian_disk
  - /callbacks@_global_: disk_with_velocity
  - _self_

function_generator:
  radial: True

model_state_kwargs:
  num_steps: 200

n_epochs: 500000
