

(1D PCA)
[] Make non-yellow
[] Fix aspect ratio of the PCA stuff
[] the residual energy plot is stretched

(2D PCA)
[] Aspect ratio of Energy plots
[] Add the row in reconstruction

(Volume preserving)
[] Fix the aspect ratio plot with the chart

[] Sort the PCA bases according to their explained variance and also print out the explained variance
[] Run the Taylor-green vortex example  in the background

--------

[] Create a very simple Laplacian experiment as the running example
[] Add multiple options for the PMF of Infidictionary, apart from the geometric with a shell,
    try adding an index sampler that samples proportional to (1/f)^d where f is the L^2 of the multi-index
[] Make coefficient model and index model separate objects from the infidictionary but tightly linked to it.
[] Test if everything works on NTK, FPCA, heat equation, and concept basis.
[] Remove all of the dead code and all of the other architectures, just keep the debugging architecture,
    the distorted Fourier architecture, and the old architecture that performs well
[] Re-run the experiments with the dead code gone

[] Try to get a good result with heat equation or remove it
[] Get a two-moons result for NTK interpretability and replace it with the current thing I have
[] Get the concept basis experiment working
[] Get a good TV basis

[] Check if all of the notebooks work or not
[] Start writing stuff (in parallel)
[] Laplacian eigenfunctions (Shape Space Spectra)
[] Merge the refactor changes


Side Experiments:
[] For the Monte-carlo methods, try a mix of pushforward pullback rather than just pushforward
[] Implement the Adjoint method of the Cayley ODE solver in case I increase the number of steps
[] Use a simple Haar wavelet basis instead of the Fourier basis for the celeba example.
